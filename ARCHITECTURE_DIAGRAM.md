```
╔═══════════════════════════════════════════════════════════════════════════╗
║                    ARCHITECTURE DIAGRAM - RADAR SYSTEM                    ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│  TAB 3: INFO CUACA (Weather List)                                       │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ [Wilayah 1: Sleman - Hujan Petir - 26°C] [Lihat Peta Radar]   │   │
│  │ [Wilayah 2: Bantul - Cerah - 28°C]      [Lihat Peta Radar]   │   │
│  │ [Wilayah 3: Kulon Progo - Berawan]      [Lihat Peta Radar]   │   │
│  │ ...                                                            │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                          │                                               │
│                          │ onClick "Lihat Peta Radar"                   │
│                          │                                               │
│                          ↓                                               │
│  router.push({                                                          │
│    pathname: '/(tabs)',                                                 │
│    params: {                                                            │
│      lat: -7.77,          // Center latitude                           │
│      long: 110.39,        // Center longitude                          │
│      highlightId: 'sleman'  // ID wilayah yang di-highlight           │
│    }                                                                    │
│  })                                                                     │
└─────────────────────────────────────────────────────────────────────────┘
                                ║
                                ║ NAVIGATION
                                ║
┌─────────────────────────────────────────────────────────────────────────┐
│  TAB 1: PETA RADAR (Map View)                                           │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  useLocalSearchParams() CAPTURE PARAMS                         │  │
│  │  {                                                             │  │
│  │    lat: -7.77,                                                │  │
│  │    long: 110.39,                                              │  │
│  │    highlightId: 'sleman'                                      │  │
│  │  }                                                             │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│              ↓                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  mapRef.current.animateToRegion({                               │  │
│  │    latitude: -7.77,                                            │  │
│  │    longitude: 110.39,                                          │  │
│  │    latitudeDelta: 0.08,   // Zoom level                        │  │
│  │    longitudeDelta: 0.08                                        │  │
│  │  }, 1000);  // 1 second smooth animation                       │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│              ↓                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  setActiveRegionId('sleman')                                    │  │
│  │  ↓                                                              │  │
│  │  MapView RERENDER dengan activeRegionId = 'sleman'            │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│              ↓                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  DATA_DIY.map((region) => {                                    │  │
│  │    const isHighlighted = activeRegionId === region.id         │  │
│  │                                                                │  │
│  │    const colors = getPolygonColor(region.id, isHighlighted)   │  │
│  │                                                                │  │
│  │    return <Polygon                                             │  │
│  │      fillColor={colors.fillColor}    ← DYNAMIC COLOR           │  │
│  │      strokeColor={colors.strokeColor}                         │  │
│  │      strokeWidth={colors.strokeWidth}                         │  │
│  │    />                                                           │  │
│  │  })                                                             │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  getPolygonColor() LOGIC:                                               │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                                                                 │  │
│  │  if (isHighlighted) {                                           │  │
│  │    return {                                                     │  │
│  │      fillColor: isBlinking                                      │  │
│  │        ? 'rgba(255, 0, 0, 0.5)'    ← BRIGHT RED              │  │
│  │        : 'rgba(255, 0, 0, 0.1)',   ← DIM RED                 │  │
│  │      strokeColor: 'rgba(255, 0, 0, 1)',  ← SOLID RED BORDER  │  │
│  │      strokeWidth: 3                                            │  │
│  │    };                                                           │  │
│  │  }                                                              │  │
│  │                                                                 │  │
│  │  // WEATHER-BASED COLOR                                        │  │
│  │  const weather = weatherData.find(w => w.id === regionId)     │  │
│  │  const desc = weather?.weatherDesc?.toLowerCase()              │  │
│  │                                                                 │  │
│  │  if (desc.includes('hujan')) {                                 │  │
│  │    return { fillColor: 'rgba(0, 100, 200, 0.4)', ... }       │  │
│  │  }                                                              │  │
│  │  if (desc.includes('cerah')) {                                 │  │
│  │    return { fillColor: 'rgba(255, 200, 0, 0.3)', ... }       │  │
│  │  }                                                              │  │
│  │  if (desc.includes('berawan')) {                               │  │
│  │    return { fillColor: 'rgba(150, 150, 150, 0.3)', ... }     │  │
│  │  }                                                              │  │
│  │                                                                 │  │
│  │  return { fillColor: 'rgba(0, 0, 0, 0.05)', ... }            │  │
│  │                                                                 │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  BLINKING ANIMATION:                                                    │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  useEffect(() => {                                              │  │
│  │    if (activeRegionId) {                                        │  │
│  │      const interval = setInterval(() => {                      │  │
│  │        setIsBlinking(prev => !prev);  ← TOGGLE EVERY 500ms    │  │
│  │      }, 500);                                                  │  │
│  │      return () => clearInterval(interval);  ← CLEANUP         │  │
│  │    }                                                             │  │
│  │  }, [activeRegionId]);                                          │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  Timeline:                                                              │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ T=0s:   Region clicked, highlight start, first blink           │  │
│  │         activeRegionId='sleman', isBlinking=true               │  │
│  │                                                                │  │
│  │ T=0.5s: Blink interval fires                                  │  │
│  │         isBlinking=false → fillColor dim red                  │  │
│  │                                                                │  │
│  │ T=1.0s: Blink interval fires again                            │  │
│  │         isBlinking=true → fillColor bright red                │  │
│  │                                                                │  │
│  │ ... (continue blink pattern)                                  │  │
│  │                                                                │  │
│  │ T=5.0s: AUTO TIMEOUT TRIGGERED                                │  │
│  │         setTimeout(() => {                                    │  │
│  │           setActiveRegionId(null)  ← STOP HIGHLIGHTING        │  │
│  │         }, 5000);                                             │  │
│  │                                                                │  │
│  │ T>5.0s: Polygon return to normal weather color               │  │
│  │         Blink effect stop                                     │  │
│  │         Info overlay disappear                                │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  INFO OVERLAY (Conditional Render):                                    │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ {activeRegionId && (                                            │  │
│  │   <View style={styles.infoOverlay}>                            │  │
│  │     <Text>Kabupaten Sleman</Text>                              │  │
│  │     <Text>Hujan Petir</Text>                                   │  │
│  │     <Text>26°C</Text>                                          │  │
│  │   </View>                                                      │  │
│  │ )}                                                              │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │                 PETA VISUAL                                   │   │
│  │                                                                │   │
│  │   ╔════════════════════════════════════════════════╗          │   │
│  │   ║                                                ║          │   │
│  │   ║     Polygon 1 (Sleman)    ← BLINKING RED     ║          │   │
│  │   ║     ╔─────────────┐                          ║          │   │
│  │   ║     │  ●  ●  ●   │                          ║          │   │
│  │   ║     │ ●       ●  │  ← Polygon Fill + Stroke║          │   │
│  │   ║     │  ●  ●  ●   │                          ║          │   │
│  │   ║     └─────────────┘                          ║          │   │
│  │   ║                          [Markers]          ║          │   │
│  │   ║     [Other Polygons in normal colors]       ║          │   │
│  │   ║                                                ║          │   │
│  │   ║         [FAB Button +]                        ║          │   │
│  │   ╚════════════════════════════════════════════════╝          │   │
│  └────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

╔═══════════════════════════════════════════════════════════════════════════╗
║                           STATE MANAGEMENT FLOW                           ║
╚═══════════════════════════════════════════════════════════════════════════╝

STATE VARIABLES:
┌─────────────────────────────────────────────────────────────────────────┐
│  activeRegionId: string | null = null                                    │
│  ├─ null → Normal state, no highlighting                               │
│  └─ 'sleman' → Sleman region is highlighted & blinking                │
│                                                                         │
│  isBlinking: boolean = false                                            │
│  ├─ true → Polygon fill opacity high (0.5)                            │
│  └─ false → Polygon fill opacity low (0.1)                            │
│                                                                         │
│  weatherData: Array<{id, weatherDesc, temp, ...}> = []                │
│  ├─ Fetched dari BMKG API pada mount                                  │
│  └─ Used untuk getPolygonColor() logic                                │
│                                                                         │
│  params: {lat?, long?, highlightId?} = {}                             │
│  ├─ Captured dari router.push() dari Tab 3                            │
│  └─ Triggers animation & highlight setup                             │
└─────────────────────────────────────────────────────────────────────────┘

REFS:
┌─────────────────────────────────────────────────────────────────────────┐
│  mapRef: React.Ref<MapView>                                              │
│  └─ Used untuk mapRef.current.animateToRegion()                        │
└─────────────────────────────────────────────────────────────────────────┘

╔═══════════════════════════════════════════════════════════════════════════╗
║                              DATA SOURCES                                 ║
╚═══════════════════════════════════════════════════════════════════════════╝

1. LOCAL: constants/jogjaData.js (DATA_DIY)
   ├─ 5 regions dengan coordinates polygon & center
   └─ Static data (tidak berubah)

2. EXTERNAL: BMKG API
   ├─ GET /prakiraan-cuaca?adm4=34.04.11.2003
   └─ Realtime weather: desc, temp, humidity

3. EXTERNAL: Firebase Realtime Database
   ├─ points/ collection dengan markers
   └─ User-created location data

4. INTERNAL: Navigation Params (useLocalSearchParams)
   ├─ From Tab 3 when user click "Lihat Peta"
   └─ Contains: lat, long, highlightId

```

---

## 🎯 KEY INSIGHTS

1. **Separation of Concerns**
   - Tab 3: Data fetching & list display
   - Tab 1: Visualization & animation
   - Constants: Shared data definitions

2. **State Synchronization**
   - Navigation params trigger state changes
   - State changes trigger re-renders
   - Re-renders apply new colors & animations

3. **Performance Optimization**
   - Promise.all() untuk parallel API calls
   - Cleanup functions untuk prevent memory leak
   - Conditional rendering untuk overlay

4. **User Experience**
   - Smooth 1s animation ke target lokasi
   - Visual feedback via blinking effect
   - Auto-timeout untuk prevent UI clutter
   - Info overlay untuk instant data access

